# ============================================================
# FRONTEND ENVIRONMENT VARIABLES (Vite)
# Add these to your .env file in the project root
# ============================================================

# Supabase Configuration (Required)
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-key"
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# Cloudflare Turnstile (DDoS Protection)
# Get keys from https://dash.cloudflare.com/?to=/:account/turnstile
VITE_TURNSTILE_SITE_KEY="your-site-key-here"

# Sentry Error Monitoring (Optional)
# Sign up at https://sentry.io and add your DSN here
# VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/your-project-id

# PostHog Analytics (Optional)
# Sign up at https://posthog.com and add your API key here
# VITE_POSTHOG_KEY=phc_your_api_key_here
# VITE_POSTHOG_HOST=https://app.posthog.com

# ============================================================
# BACKEND ENVIRONMENT VARIABLES (Supabase Edge Functions)
# Add these via Supabase CLI or Dashboard → Edge Functions → Secrets
# DO NOT prefix these with VITE_ - they are server-side only
# ============================================================

# Cloudflare Turnstile Secret (Required for server-side verification)
# Command: supabase secrets set TURNSTILE_SECRET_KEY=your-secret-key
# TURNSTILE_SECRET_KEY="your-secret-key-here"

# Note: SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are automatically
# available in Edge Functions - you don't need to set them manually

